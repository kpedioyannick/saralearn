<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title|e }}</title>
    <style>
        body { font-family: sans-serif; max-width: 600px; margin: 3em auto; padding: 0 1em; }
        h1 { color: #333; }
        p { color: #666; }
    </style>
</head>
<body>
    <h1>{{ title|e }}</h1>
    <p>{{ message|e }}</p>
</body>
</html>
